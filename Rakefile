# -*- ruby -*-

require 'rubygems'
require 'hoe'
require './lib/map_fields.rb'

Hoe.new('map-fields', MapFields::VERSION) do |p|
  p.rubyforge_name = 'map-field'
  p.developer('Andrew Timberlake', 'andrew@andrewtimberlake.com')
end

# vim: syntax=Ruby
